<nav class="navbar navbar-expand-lg navbar-dark bg-dark navbar-light">
    <div class="container-fluid">
            <div class="rounded-circle overflow-hidden" style="width: 40px; height: 40px;">
                <%= image_tag('logo.png', alt: 'JetMaintain', class: 'img-fluid') %>
            </div>
        <span class="navbar-brand">JetMaintain</span>
        <%= link_to 'Inicio', pages_inicio_path, class: 'navbar-brand' %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <%= link_to 'Termininos', pages_terminos_path, class: 'nav-link text-white' %>
                </li>
                <li class="nav-item">
                    <%= link_to 'Mantenimiento', maintenances_path, class: 'nav-link text-white' %>
                </li>
                <li class="nav-item">
                    <%= link_to 'Motores', engines_path, class: 'nav-link text-white' %>
                </li>
            </ul>
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <% if user_signed_in? %>
                            <% if current_user.email.present? %>
                                Bienvenid@! <%= current_user.email %>
                            <% else %>
                                Hola! <%= current_user.email %>
                            <% end %>
                        <% else %>
                            Registrate o conectate
                        <% end %>
                    </a>
                    <ul class="dropdown-menu">
                        <% if user_signed_in? %>
                            <li><%= button_to 'Cerrar sesión', destroy_user_session_path, class: 'dropdown-item', method: :delete %></li>
                        <% else %>
                            <li><%= link_to 'Iniciar sesión',  new_user_session_path, class: 'dropdown-item'%></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><%= link_to 'Registro',  new_user_registration_path, class: 'dropdown-item'%></li>
                        <% end %>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>
